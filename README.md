
  # Ramadhan Productivity Tracker App

  This is a code bundle for Ramadhan Productivity Tracker App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  