// Contoh tipe data user (sesuaikan dengan kebutuhan nanti)
export interface UserProfile {
  id: string;
  email: string;
  full_name?: string;
  avatar_url?: string;
}

// Tambahkan interface untuk tabel lain (misal: Agenda, Tracker) di sini nanti